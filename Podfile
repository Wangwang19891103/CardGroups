# Uncomment this line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'CardGroups' do
  pod 'Firebase’
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  pod 'Firebase/Auth'
  

  # Pods for CardGroups

  target 'CardGroupsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CardGroupsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
